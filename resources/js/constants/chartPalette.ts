/**
 * Hex palette aligned with app theme (brand, ink, success, warning, destructive).
 * ApexCharts does not reliably resolve CSS `oklch()` in all contexts.
 */
export const CHART_PALETTE_HEX = ['#5b63f5', '#1e293b', '#16a34a', '#ca8a04', '#dc2626', '#7c3aed'];
